// Navigation entry point for application code. Flux uses Inertia-style
// server-driven routing — the Go router owns the URLs and this client
// swaps Vue pages in place. Import { router, Link } from '@/router'.
export { router, Link } from './flux'
export type { VisitOptions } from './flux'
